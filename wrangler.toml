#:schema node_modules/wrangler/config-schema.json
name = "sendoomi-app"
compatibility_date = "2024-04-01"
pages_build_output_dir = "dist"

[vars]
ENVIRONMENT = "production"

# Note: In the future, we will add [d1_databases] and [kv_namespaces] here.
